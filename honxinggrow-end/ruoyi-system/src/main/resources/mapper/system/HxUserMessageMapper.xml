<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.HxUserMessageMapper">
    
    <resultMap type="HxUserMessage" id="HxUserMessageResult">
        <result property="messageId"    column="message_id"    />
        <result property="userId"    column="user_id"    />
        <result property="title"    column="title"    />
        <result property="content"    column="content"    />
        <result property="type"    column="type"    />
        <result property="isRead"    column="is_read"    />
        <result property="createTime"    column="create_time"    />
    </resultMap>

    <sql id="selectHxUserMessageColumns">
        select message_id, user_id, title, content, type, is_read, create_time from hx_user_message
    </sql>

    <select id="selectHxUserMessageList" parameterType="HxUserMessage" resultMap="HxUserMessageResult">
        <include refid="selectHxUserMessageColumns"/>
        <where>  
            <if test="userId != null "> and user_id = #{userId}</if>
            <if test="title != null  and title != ''"> and title like concat('%', #{title}, '%')</if>
            <if test="type != null  and type != ''"> and type = #{type}</if>
            <if test="isRead != null  and isRead != ''"> and is_read = #{isRead}</if>
        </where>
        order by create_time desc
    </select>
    
    <select id="selectHxUserMessageByMessageId" parameterType="Long" resultMap="HxUserMessageResult">
        <include refid="selectHxUserMessageColumns"/>
        where message_id = #{messageId}
    </select>
        
    <insert id="insertHxUserMessage" parameterType="HxUserMessage" useGeneratedKeys="true" keyProperty="messageId">
        insert into hx_user_message
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="title != null">title,</if>
            <if test="content != null">content,</if>
            <if test="type != null">type,</if>
            <if test="isRead != null">is_read,</if>
            <if test="createTime != null">create_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="title != null">#{title},</if>
            <if test="content != null">#{content},</if>
            <if test="type != null">#{type},</if>
            <if test="isRead != null">#{isRead},</if>
            <if test="createTime != null">#{createTime},</if>
         </trim>
    </insert>

    <update id="updateHxUserMessage" parameterType="HxUserMessage">
        update hx_user_message
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="type != null">type = #{type},</if>
            <if test="isRead != null">is_read = #{isRead},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
        </trim>
        where message_id = #{messageId}
    </update>

    <delete id="deleteHxUserMessageByMessageId" parameterType="Long">
        delete from hx_user_message where message_id = #{messageId}
    </delete>

    <delete id="deleteHxUserMessageByMessageIds" parameterType="String">
        delete from hx_user_message where message_id in 
        <foreach item="messageId" collection="array" open="(" separator="," close=")">
            #{messageId}
        </foreach>
    </delete>

    <select id="countUnreadMessages" parameterType="Long" resultType="int">
        select count(1) from hx_user_message where user_id = #{userId} and is_read = '0'
    </select>

    <delete id="deleteHxUserMessageByUserId" parameterType="Long">
        delete from hx_user_message where user_id = #{userId}
    </delete>
</mapper>
