FROM node:16-bullseye-slim

WORKDIR /app
COPY package*.json ./
RUN npm ci

EXPOSE 80
ENV CHOKIDAR_USEPOLLING=true

CMD ["bash", "-lc", "if [ ! -d node_modules ]; then npm ci; fi; npm run dev -- --host 0.0.0.0 --port 80 --open false"]

