# 修复活动参与人数和故事参数显示问题

## 问题分析

1. **字段名不匹配**：后端SQL返回下划线命名（如`view_count`），前端期望驼峰命名（如`viewCount`）
2. **未定义变量错误**：模板中存在未定义的`className`和`height`变量，导致控制台报错
3. **数据处理缺失**：需要确保数据正确转换和处理

## 修复方案

### 1. 修复首页模板问题

**修改文件**：`ruoyi-ui/src/views/index.vue`

- **移除**第106行的空div元素：`<div :class="className" :style="{height:height,width:width}" />`
- 确保所有属性名使用正确的驼峰命名
- 添加字段名转换逻辑，将后端返回的下划线命名转换为驼峰命名

### 2. 添加数据转换逻辑

在`fetchDashboardData`方法中添加数据转换逻辑，将后端返回的下划线命名字段转换为驼峰命名：

```javascript
// 将下划线命名转换为驼峰命名
const convertKeysToCamel = (obj) => {
  if (!obj || typeof obj !== 'object') return obj;
  if (Array.isArray(obj)) return obj.map(item => convertKeysToCamel(item));
  
  const result = {};
  for (const key in obj) {
    if (obj.hasOwnProperty(key)) {
      const camelKey = key.replace(/_([a-z])/g, (match, letter) => letter.toUpperCase());
      result[camelKey] = convertKeysToCamel(obj[key]);
    }
  }
  return result;
};
```

### 3. 确保数据正确处理

- 修复列表数据中的字段名匹配问题
- 确保图表数据正确传递给图表组件
- 添加错误处理机制，防止数据获取失败导致页面崩溃

## 修复后的预期效果

1. 活动参与人数正确显示
2. 故事阅读量和点赞数正确显示
3. 图表数据正确渲染
4. 控制台无报错
5. 页面加载正常

## 修复步骤

1. 修改`ruoyi-ui/src/views/index.vue`，移除未定义的变量引用
2. 添加数据转换函数，将下划线命名转换为驼峰命名
3. 更新数据处理逻辑，确保所有字段名正确匹配
4. 测试页面加载和数据显示效果

## 技术要点

- 使用JavaScript实现下划线转驼峰的字段名转换
- 确保模板中只使用已定义的变量
- 增强数据处理的健壮性，防止undefined值导致的错误
- 遵循Vue组件开发最佳实践