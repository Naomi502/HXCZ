既然找不到插件（可能是因为小程序主体类型限制），那我们**彻底放弃插件方案**，改用**后端直连阿里云语音服务**。

### 核心优势
1. **零插件**：您**不需要**在微信后台做任何操作，也不受小程序类目限制。
2. **复用配置**：直接复用您刚刚配置的“通义千问”API Key（如果您使用的是阿里云/通义千问）。
3. **音质更好**：阿里云 CosyVoice 大模型语音合成的效果比微信插件更自然。

### 实施方案

1. **后端开发 (Java)**：
   * 在 `HxAiService` 中增加 TTS 接口，直接调用阿里云 DashScope 语音合成 API。
   * **注意**：这需要您在后台“AI 参数配置”中填入阿里云的 API Key（如果您还在用智谱，可能需要切换或单独填 Key）。

2. **前端开发 (小程序)**：
   * **流式切分逻辑**：保留之前的设计，AI 边打字，前端边按标点切分。
   * **请求后端**：切分好的短句发送给后端 `/hx/ai/tts` 接口。
   * **队列播放**：接收后端返回的音频数据并自动播放。

3. **使用条件**：
   * 此方案依赖**阿里云 DashScope** 服务。代码更新后，请确保您的 AI 配置中使用的是有效的阿里云 API Key。

我将立即开始修改前后端代码来实现这一功能。
