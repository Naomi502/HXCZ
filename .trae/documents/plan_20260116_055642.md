### 问题分析

1. **热门问答卡片名称需要改为最新问答**
2. **首页的热门问答和心灵树洞卡片不显示内容**

### 原因分析

从查看的代码来看，主要问题可能是：

1. **数据字段不匹配**：
   - 问答管理页使用的是 `qaId`、`title`、`status`、`viewCount` 等字段
   - 心灵树洞管理页使用的是 `treeHoleId`、`userId`、`userName`、`content`、`status` 等字段
   - 首页可能期望的是不同的字段名，或者数据结构不一致

2. **API 调用问题**：
   - 首页使用 `getDashboardData()` 获取所有数据
   - 问答和心灵树洞管理页分别使用 `listQa()` 和 `listHole()` 获取数据
   - 可能存在数据返回格式不一致的问题

### 修复方案

1. **修改热门问答卡片名称**：
   - 将首页的"热门问答"卡片名称改为"最新问答"

2. **修复问答卡片数据显示**：
   - 确保首页问答卡片使用正确的字段名
   - 检查 `topQa` 数据的结构和字段名
   - 确保表格列与数据字段匹配

3. **修复心灵树洞卡片数据显示**：
   - 确保首页心灵树洞卡片使用正确的字段名
   - 检查 `recentTreeHole` 数据的结构和字段名
   - 确保表格列与数据字段匹配

4. **统一数据获取逻辑**：
   - 参考问答和心灵树洞管理页的获参功能
   - 确保首页数据获取逻辑与管理页一致

### 具体修改

1. **修改卡片名称**：
   - 将 `<span>热门问答</span>` 改为 `<span>最新问答</span>`

2. **检查并修复数据字段**：
   - 检查首页使用的数据字段是否与后端返回一致
   - 如有必要，在 `convertKeysToCamel` 方法中添加字段映射
   - 确保表格列的 `prop` 属性与数据字段匹配

3. **优化数据处理**：
   - 确保 `topQa` 和 `recentTreeHole` 数据正确转换和处理
   - 检查数据长度限制逻辑是否正确

### 预期效果

- 热门问答卡片名称改为最新问答
- 首页的最新问答卡片显示正确的内容
- 首页的心灵树洞卡片显示正确的内容
- 数据字段匹配，表格显示正常

### 实施步骤

1. 首先修改热门问答卡片的名称
2. 检查并修复问答卡片的数据字段
3. 检查并修复心灵树洞卡片的数据字段
4. 测试修复效果，确保卡片正常显示内容